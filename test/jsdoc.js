/* eslint-disable jsdoc/tag-lines, unicorn/no-empty-file */
/**
 * In these configuration files, the rule plugin1/rule1 comes from the plugin
 * named plugin1. You can also use this format with configuration comments, such as:
 *
 * Hello.
 *
 * @example
 *   ```html
 *           <h1>test</h1>
 *   ```;
 *
 * @type {React.FC<{ message: string }>}
 *
 * @param x Test
 * @param {any} var An example text that is long and spans lines followed by a list
 *
 *   - Item 1.
 *   - Item 2.
 *   - Item 3.
 *
 *   And more text to come.
 *
 * @returns {Promise} A return value.
 */
